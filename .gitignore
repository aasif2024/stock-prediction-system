# Python
__pycache__/
*.pyc
venv/
.venv/

# Environment secrets
backend/.env
frontend/.env.development

# SQLite database (runtime data — do not commit)
database/stock_prediction.db
backend/database/stock_prediction.db

# Node / React
frontend/node_modules/
frontend/build/

# ML artifacts (only ignore large training-time artifacts; backend pkl files are needed for deployment)
machine_learning/saved_model/*.pkl
# backend/ml_model/*.pkl  <-- intentionally NOT ignored so Render can serve predictions

# OS
.DS_Store
